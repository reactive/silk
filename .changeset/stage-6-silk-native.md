---
'@reactive/silk-native': minor
---

Introduce `@reactive/silk-native`: React Native renderer for Silk with context-based `ThemeProvider` / `SilkProvider`, style mappers from `silk-core` recipes, and exemplar components `Box`, `Stack`, `Inline`, `Text`, and `Button`. Same semantic Theme object and recipe contracts as web; no CSS variables.
