---
"@reactive/silk-core": minor
"@reactive/silk": minor
---

Stage 1 layout system: `Inline`, `Grid`, `Container`, `Separator`, and `Box` padding; system density via `compactSpace` + `data-density` space remaps; web-only `collapseBelow` container queries; ThemeProvider density / nested theme inheritance.
