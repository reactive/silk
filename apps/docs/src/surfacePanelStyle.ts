import type { CSSProperties } from 'react';

/** Neutral raised panel chrome for docs stories (not a Silk component). */
export const surfacePanelStyle: CSSProperties = {
  padding: 'var(--silk-space-4)',
  borderRadius: 'var(--silk-radius-md)',
  border: '1px solid var(--silk-color-border-subtle)',
  backgroundColor: 'var(--silk-color-surface)',
  color: 'var(--silk-color-text-primary)',
};
