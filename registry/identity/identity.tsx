/**
 * Identity composite — distributed via the Silk registry.
 * Imports primitives from the published `@reactive/silk` package.
 *
 * Install: `npx shadcn@latest add https://github.com/<org>/silk/identity`
 * (or via GitHub registry once the repo is public)
 */
export {
  Identity,
  type IdentityAvatarProps,
  type IdentityMetaProps,
  type IdentityNameProps,
  type IdentityProps,
  type IdentityRootProps,
} from '@reactive/silk';
